# Server Configuration
server.port=${PORT:8080}

# Database (Neon PostgreSQL)
spring.datasource.url=postgresql://neondb_owner:npg_nvXY0bUIBu6q@ep-holy-tree-am3b33xt.c-5.us-east-1.aws.neon.tech/neondb?sslmode=require
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Prevent startup crash if DB delay
spring.jpa.defer-datasource-initialization=true
spring.datasource.hikari.initialization-fail-timeout=0

# Application Name
spring.application.name=expense-tracker

# JWT Configuration
jwt.secret=ExpenseTrackerSecretKey2024SuperSecureKeyForJWTTokenGeneration
jwt.expiration=86400000